# Firebase credentials

Put your Firebase service account key here:

- **Filename:** `serviceAccountKey.json`
- **How to get it:** Firebase Console → Project settings → Service accounts → Generate new private key

Do not commit this file. Add `config/serviceAccountKey.json` to `.gitignore`.
